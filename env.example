# Copy to .env and fill in values
# Password for visual-regression test users (set by setup-users script)
VR_PASSWORD=visualregression123

# Base URL for the VNDLY app (your local tenant subdomain)
VR_BASE_URL=https://cruise.localqa.vndly.com

# Viewport dimensions for screenshots
VR_VIEWPORT_WIDTH=1440
VR_VIEWPORT_HEIGHT=900
